# Harberger-Title NFT（哈伯格税社区头衔 NFT）

> **Mini Demo 0** — 一个基于哈伯格税机制的社区稀缺头衔 NFT 概念验证项目。

---

## 1. 做了什么

### 核心机制

本项目实现了一个基于 **哈伯格税（Harberger Tax）** 的 NFT 合约。简单来说：

- **持有者自报价**：NFT 持有者必须为自己的头衔设定一个"卖价"。
- **强制买断权**：任何人都可以按这个价格直接买走 NFT，持有者无法拒绝。
- **持续纳税**：持有者必须按年化税率持续为其自报价支付税金（以 ERC-20 代币支付）。
- **违约回收**：如果税金耗尽，NFT 进入违约状态，任何人可以低价或免费申领。

### 它解决了什么问题？

在传统的 NFT 或社区头衔系统中，存在两个常见问题：

| 问题 | 传统系统的症状 | 哈伯格税的解法 |
|------|--------------|--------------|
| **占坑不拉屎（Squatting）** | 早期用户抢到稀缺头衔后永远不卖，即使他们已经不活跃 | 税金机制迫使持有者持续"付费"维持所有权。如果你不珍惜这个头衔，税金会把你赶走。 |
| **流动性僵化** | 想买的人买不到，想卖的人不知道定什么价 | 强制买断权 + 自报价机制，让市场始终有一个清晰的价格信号。持有者在"报高价怕被买走"和"报低价少交税"之间博弈，价格自然趋向合理。 |

### 一句话总结

> **哈伯格税让稀缺资产的分配效率最大化：资产永远属于"最愿意为它付费"的人。**

---

## 2. 真实链上操作

以下操作需要在测试网（如 Monad Testnet 或 Sepolia）上真实发送交易并产生 Tx Hash：

| # | 操作 | 函数调用 | 是否需要 Gas | 是否需要 Approve |
|---|------|---------|-------------|-----------------|
| 1 | 授权合约使用代币 | `MockERC20.approve(contract, amount)` | ✅ | — |
| 2 | 铸造 NFT 并充入押金 | `HarbergerTitleNFT.mint(price, deposit)` | ✅ | 需要先完成 #1 |
| 3 | 充值税金押金 | `HarbergerTitleNFT.depositCollateral(amount)` | ✅ | 需要先 Approve |
| 4 | 更新自报价 | `HarbergerTitleNFT.setPrice(newPrice)` | ✅ | 不需要 |
| 5 | 强制买断 | `HarbergerTitleNFT.buyout(price)` | ✅ | 需要先 Approve |
| 6 | 申领违约 NFT | `HarbergerTitleNFT.claimForeclosed(price)` | ✅ | 需要先 Approve（如果 floorPrice > 0） |

**查询操作**（不产生交易，不消耗 Gas）：
- `owedTax()` — 查询当前欠税
- `foreclosed()` — 查询是否违约
- `netEscrowBalance()` — 查询净押金余额
- `selfAssessedPrice()` — 查询当前自报价

---

## 3. AI 辅助部分

以下内容由 AI（Claude）辅助生成，已标注具体范围：

| 辅助内容 | 说明 |
|---------|------|
| **Solidity 惰性求值算法初稿** | `_calculateOwedTax()` 和 `_settleTax()` 的核心逻辑，包括时间戳差值计算和基点转换公式 |
| **数学公式的时间精度转换** | 将年化税率（如 10%/年）转换为基于秒的精确计算，处理 Solidity 整数运算的精度问题（先乘后除） |
| **合约安全模式设计** | Escrow 托管模式 vs. Allowance 自动扣款模式的权衡分析；transferFrom/safeTransferFrom 的覆写以防止绕过强制出售权 |
| **README 框架生成** | 本文档的整体结构和各章节的初始内容 |
| **交互流程的数值示例** | 场景一和场景二中的具体数值计算和资金流向分析 |

---

## 4. 人工判断和修改

作为人类开发者，你在本 Demo 中需要做出以下关键决策和安全修改：

### 4.1 税金去向：国库 vs. 销毁

**当前设计**：税金留在合约中（Escrow 托管），买断时扣除。

**你需要决定**：
- **选项 A：税金归入国库（Treasury）** — 可以用于社区基金、开发资助等。需要添加 `withdrawTax()` 函数和权限控制。
- **选项 B：税金直接销毁（Burn）** — 减少代币流通量，增加代币价值通缩。但可能引发"虚假交易"攻击（持有者用低价自买自卖来销毁代币）。
- **选项 C：税金分配给社区其他持币者** — 形成二次博弈，激励更多人参与。

> **建议**：Demo 阶段先用 Escrow 托管模式（当前实现），后续根据代币经济学目标选择。

### 4.2 Escrow 托管 vs. Allowance 自动扣款

**当前设计**：Escrow 模式——持有者需要主动充值押金到合约中。

**为什么不用 Allowance 模式？**（即：合约直接从持有者钱包中扣款）
- Allowance 模式下，持有者可以随时撤销授权（`approve(0)`），导致税金无法扣除，形成"逃税"。
- Escrow 模式下，资金已经锁在合约中，持有者无法单方面撤回，保证了税金的确定性。

> **建议**：保持 Escrow 模式。如果用户体验是优先考虑，可以在前端做"一键充值"的 UX 优化。

### 4.3 年化税率参数

**当前设计**：税率在构造函数中设定，不可更改。

**你需要决定**：
- **税率高低**：10% 是一个常见的起点。太高会导致持有成本过高，太低则无法有效防止占坑。
- **是否可调整**：是否需要一个 `setTaxRate()` 函数让治理合约（Governor）动态调整税率？
- **税率上限**：建议设置一个硬编码的上限（如 50%），防止治理攻击将税率设为 100%。

### 4.4 违约申领的价格

**当前设计**：`floorPrice` 在构造函数中设定，违约后以此价格申领。

**你需要决定**：
- **免费申领（floorPrice = 0）**：最大化开放性，但可能导致"抢跑"（Frontrunning）——MEV 机器人会抢先申领。
- **基准价申领（floorPrice > 0）**：有一定门槛，减少机器人抢跑，但也降低了"社区回收"的效率。

### 4.5 安全审计要点

在将合约部署到主网之前，建议重点审查：

- [ ] **重入攻击（Reentrancy）**：`buyout()` 中的代币转移是否可能被恶意合约重入？（SafeERC20 + 状态更新在转账之后可以缓解）
- [ ] **整数溢出**：`_calculateOwedTax()` 中的乘法是否会溢出 uint256？（在极端参数下需要检查）
- [ ] **精度损失**：先乘后除的运算顺序是否会导致中间结果溢出？（需要根据实际参数范围验证）
- [ ] **时间戳操纵**：矿工是否可以通过操纵 `block.timestamp` 来影响税金计算？（通常影响在 15 秒以内，可接受）

---

## 5. 下一步推进方向

### 方向 A：税金分红（Tax Redistribution）

将税金按比例分配给社区其他持币者，形成**二次博弈**：

- 持有者 A 交的税，分给其他 TOKEN 持有者
- 这会激励更多人持有 TOKEN（因为持有 TOKEN 可以获得"被动收入"）
- 形成正向飞轮：更多人持有 TOKEN → TOKEN 需求增加 → 价格上升 → 持有者更愿意交税

### 方向 B：动态税率衰减（Dynamic Tax Decay）

引入时间维度的税率变化：

- 新铸造的 NFT 税率较低（鼓励早期参与）
- 随着持有时间增加，税率逐渐上升（防止长期占坑）
- 或者：当 NFT 的买断次数越多（说明越稀缺），税率自动上调

### 方向 C：社交协议集成

将哈伯格头衔与社交协议（如 Farcaster、Lens Protocol）结合：

- 头衔持有者的社交资料上显示特殊标识
- 头衔可以在社交协议中解锁专属功能（如：专属频道、优先发言权）
- 前端实现"一键挑战"按钮，降低买断操作的门槛

---

## 项目结构

```
mini-demo-0/
├── contracts/
│   ├── MockERC20.sol              # 模拟 ERC-20 代币（测试用）
│   └── HarbergerTitleNFT.sol      # 哈伯格税 NFT 核心合约
├── INTERACTION_FLOW.md            # 链上交互流程详解
├── README.md                      # 本文件
└── DEPLOYMENT.md                  # 部署指南
```

---

## 许可证

MIT License — 自由使用、修改和分发。
